<%= form_for(template) do |f| %>
  <% if template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(template.errors.count, "error") %> prohibited this template from being saved:</h2>

      <ul>
      <% template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <script>
    
  </script>

  <input type="text" id="template_variaveis" name="template[variaveis]" value="" hidden>

  <div class="field">
    <%= f.label :nome, "Nome do Template" %>
    <%= f.text_field :nome%>
  </div>

  <div class="" id="variaveis"></div>

  <div class="field">
    <%= f.label :variaveis %>
    <input type="text" name="variavel" id="variavel">
    <button type="button" class="btn btn-success" onclick="addVariavel(variavel)"><i class="fa fa-plus fa-1"></i></button>
  </div>

  <div class="field">
    <%= f.label :type_id, "Tipo" %>

    <%= select("template", "type_id", Type.all.collect {|t| [ t.nome, t.id ] }, {include_blank: 'Selecione o Tipo'})%>
  </div>

  <div class="field">
    <%= f.label :texto %>
    <%= f.cktext_area :texto %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
